# Database Configuration
# PostgreSQL connection values used by the application and Docker Compose.
# Copy this file to `.env` and edit values before running.
POSTGRES_USER=simulation_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=simulation_db
POSTGRES_PORT=5432

# Example DATABASE_URL using the values above. You can set this directly
# or let the app build it from the individual POSTGRES_* variables.
# Note: some systems don't expand ${VAR} inside a .env file; keep this as an
# example rather than a required literal.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
# or for SQLite: DATABASE_URL=sqlite:///./simulation_db.sqlite

# Environment Settings
ENVIRONMENT=development
LOG_LEVEL=INFO

# Simulation Settings
DEFAULT_MAX_STEPS=1000
ENABLE_STATE_COMPRESSION=true
CHECKPOINT_INTERVAL=100